import React from "react";
import { QuestionType } from "../../../../types";

function CustomResponse({
  question,
  mobileView,
}: {
  question: QuestionType;
  mobileView: boolean;
}) {
  return <div>CustomResponse</div>;
}

export default CustomResponse;
